# skill/

**[`SKILL.md`](SKILL.md)** — the game-agnostic `marketing-reels` runbook (v0.1, 3 phases: LEARN → MAKE → VALIDATE→GENERATE).

Drive it by reading SKILL.md and executing the stages on a `<game-folder>` argument. It is a human-driven runbook (skills can't auto-chain; the lila dependencies are manual-invoke with cost gates) — Claude runs the steps and surfaces gates.

To install as a real invocable skill later: copy this folder to a skills root (or package via `skill-creator`). Authoritative design: [../discussions/2026-06-14-3phase-design.md](../discussions/2026-06-14-3phase-design.md).
