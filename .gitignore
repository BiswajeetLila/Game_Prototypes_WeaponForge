# OS / editor junk
.DS_Store
Thumbs.db
*~
*.swp
*.swo

# Editor / IDE local config
.vscode/
.idea/

# Build artifacts (none yet — single-HTML prototype)
node_modules/
dist-build/

# Note: AI-generated PNG assets (heroes, mockups) ARE tracked — they're part of the prototype.
.gstack/

# Brainstorming visual-companion scratch (ephemeral session HTML; absolute paths inside).
.superpowers/

# Godot 4 — engine import cache (regenerated on open; large, must not be tracked).
# Note: per-asset *.import sidecars and *.uid files ARE tracked (UID stability); only
# the .godot/ cache dir and Mono build output are ignored.
.godot/
.mono/
