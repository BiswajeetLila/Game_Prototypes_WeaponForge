# tactical-autobattler — empirical creative patterns
> **STUB — genre-specific pull TODO.** Run-01 (2026-06-14) queried category 6014 (all iOS Games), which returned casual/puzzle/match-3 top-spenders, NOT tactical-autobattler comparables. See [`ios-games-baseline.md`](ios-games-baseline.md) for the broad winning grammar.

## To populate this properly (next iteration)
The genre's actual comparables (Capybara Go, Wittle Defender, Brotato, Slice & Dice, AFK Journey) are not isolable via the broad Games category. Re-pull targeted:
- Use `ad_top_apps` / `search_entities` to resolve each comparable's `app_id`, then pull that app's creatives (per-advertiser), OR
- Find a narrower sub-category if the provider exposes one for autobattler/RPG.
- Networks: default to **Applovin + Unity (+ Youtube)** — Instagram/TikTok returned 0 coverage in-window (run-01 finding).

## What the broad baseline still tells us (transferable)
Even genre-agnostic, the durable winning grammar is clear and applies to our creative:
1. **Lead with an engineered hook, not a gameplay demo** — almost no long-runner opens on raw gameplay.
2. **The #1 longevity family is oddly-satisfying / ASMR cascade** — our cross-hero reaction chain ("I CAUSED THAT") is an *authentic* version of this, not a bolted-on gimmick.
3. **Pull-the-pin / choice fake-fail is proven but saturated** — usable (adapt to "which function do you socket?") but we arrive late; lead with our differentiator instead of cloning.
4. **Sound-off, captioned, 30–60s** is the dominant format.
