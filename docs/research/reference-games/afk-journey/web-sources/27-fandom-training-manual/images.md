# Images — Training Manual

Source page returned HTTP 403 Forbidden. No HTML img tags could be extracted.
The wikitext infobox does not specify an image filename; the item icon would be rendered dynamically by the Item Infobox template from the item name.

| local_file | src_url | alt_text | context |
|---|---|---|---|
| (not fetched) | (unknown — rendered dynamically by Item Infobox template) | Training Manual item icon | Item Infobox template (no explicit image= field in wikitext) |

All image fetches skipped: source page HTTP 403 prevented img tag enumeration.
